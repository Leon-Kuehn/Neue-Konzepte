# Backend Placeholder

Dies ist ein Platzhalter für zukünftige API-/Gateway-Logik (z. B. Auth, Persistenz, Digital Twins).

- `server.js` liefert derzeit nur eine simple JSON-Response.
- Start lokal: `npm install` (optional) und `npm run start` (Port 3001).
- Docker: siehe `docker-compose.yml` (Service ist vorbereitet und kann bei Bedarf aktiviert werden).
